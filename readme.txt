#instructions to run the code:

1- things to enter in the source before you run the code :: # enter your app's consumer key ,  enter app's consumer secret,
# enter access token , #access token secret ( these have been mentioned in the source as comments )

How to run the Code:: 

1 - to run using command prompt windows:
1.1- open the directory the file is saved in using the cd command
1.2- type "python filename.py keyword(to search tweet about)" NOTE- don't use the inverted commas


***NOTES :: 
#if we dont need the arguments in command prompt use the input fucntion to take a word to search relevant tweet in cmd or cli
#if we want for a keyword with multiple words (remove the parser object and use the input method)
    
# or we can use the optparse library
